"""AES helper function, to aid in the lecture.
The goal is to introduce code, show how it works, and then just call it from here.

author: bayley king
date: 2-24-2025
"""
import numpy as np
import random
import pylab as py

random.seed(42) 
np.random.seed(42)

#global variable for all HW lookups
HW = [bin(n).count("1") for n in range(0, 256)]

sbox = np.array([
        
        ],dtype=np.uint32)

#Generate a random lookup table as sbox
lookup = [x for x in range(256)]
random.shuffle(lookup)

# Create a simulated power trace, based on data in (din)
def measure_power(din, trace_len, skey, time_of_leakage):
    if time_of_leakage > trace_len or time_of_leakage < 0:
        raise ValueError("Time of Leakage needs to be greater than 0, and less than the trace length.")
    #secret byte
    
    #Calculate result
    res = lookup[din ^ skey]
    
    #Generate some arbitrary random data, not necessarily around zero
    b = 148
    a = 154    
    basetrace = (b - a) * np.random.random_sample(trace_len) + a
    
    #Determine number of 1's in result (Hamming Weight), create appropriately sized spike
    basetrace[time_of_leakage] += bin(res).count('1') - 4 # On average, HW=4, so we minus it out
        
    return basetrace

def gen_traces(number_traces, trace_len=50, skey=0b101010, time_of_leakage=35, use_sbox=False):
    """Generates traces for the lecture
    Params:
        number_traces (int): number of total traces needed.
        trace_len (int): number of time samples. default is 50.
        skey (bin): secret key value. defualt is bin(42).
        time_of_leakage (int): when in time sample HW leakage occurs. default is 35.
        use_sbox (bool): toggles using traditional sbox, or random. default is false."
    """
    if use_sbox:
        lookup = sbox
    else:
        #Generate a random lookup table as sbox
        lookup = [x for x in range(256)]
        random.shuffle(lookup)

    traces = np.array([None] * number_traces)
    input_data = np.random.randint(0, 256, number_traces)

    for d in range(0, number_traces):
        traces[d] = measure_power(input_data[d], skey=skey)
    return (input_data, traces)

    # Create a number of simulated traces and input data


def aes_internal(inputdata, key):
    """Calculates the output of an SBOX, in the AES algorithm. The input to the SBOX is the 
    current CT with the current KEY. We usually care around round 1 of the AES algorithm, 
    where this operation is directly the plaintext and the first key, before expansion.
    """
    return sbox[inputdata ^ key]